/*
Create an audio context: The first step is to create an 
AudioContext object, which provides the entry point into 
the Web Audio API. This can be done using the following code:
*/

const audioContext = new AudioContext();
